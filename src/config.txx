#pragma once

namespace cfg {
    constexpr float cameraSpeed = 20.f;
    constexpr float cameraSensetivity = 1.f;
    constexpr double targetFPS = 1000.0;
    constexpr float smoothness = 10.f;

    constexpr float rotSpeed = 360.f;
}
